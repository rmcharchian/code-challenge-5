var app = angular.module('MessageApp', ['ngRoute']);

messageApp.config(['$routeProvider', function ($routeProvider) {
    console.log('Route config loaded');

    $routeProvider.when('/',
{ })

}])

//var app = angular.model('app', []);